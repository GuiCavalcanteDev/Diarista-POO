package interfaces;

public interface Isaldo {
	
	// O que é uma interface?
    // Uma interface é como um contrato que as classes que a implementam devem seguir.
    // Sempre que criar uma interface, é uma boa prática nomeá-la com um "I" antes do nome.
	
	void sacar(double valor);
	
	void depositar(double valor);
	
	
}
